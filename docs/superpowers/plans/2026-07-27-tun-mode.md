# TUN 透明代理模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 spec `docs/superpowers/specs/2026-07-27-tun-mode-design.md`:`connect --tun` 启用分流 TUN 透明模式(root helper + NOPASSWD + scoped resolver + janitor),看门狗零改动复用,`install/uninstall --tun` 对称管理系统级组件。

**Architecture:** daemon 仍是用户态;root 只存在于 zju-connect 隧道进程(经 `sudo -n ep-tun-helper start-tunnel` 拉起)。新增 `src/tun.rs` 集中放常量、helper 脚本内容、参数校验与护栏解析(纯函数,重点单测);daemon/tunnel/lib 只做分支接线。

**Tech Stack:** Rust(clap/tokio/serde,现有依赖,不新增)、POSIX sh(helper 脚本,编译期常量)。

## Global Constraints

- 安全不变量 1:全程不改默认路由、不改全局 DNS(禁用 `-dns-hijack`);DNS 只经 `/etc/resolver/<suffix>`。
- 安全不变量 2:TUN 模式下 mixed 7899 与 `start/stop/restart` 语义完全不变;胶囊 port 段显示「tun」。
- 固定路径(不可配置):helper `/usr/local/libexec/easy-proxy/ep-tun-helper`、root copy `/usr/local/libexec/easy-proxy/zju-connect`、sudoers `/etc/sudoers.d/easy-proxy`、pidfile `/var/run/easy-proxy-tun.pid`、resolver 标记 `# managed by easy-proxy`。
- helper 参数白名单:server `^[A-Za-z0-9.-]+$`、端口纯数字、twfid `^[A-Za-z0-9]+$`、socks/http `^127\.0\.0\.1:[0-9]+$`、suffix `^[a-z0-9.-]+$`(且不含 `..`、不以 `.` 开头)、ip 必须 IPv4。
- daemon 对 root 子进程 kill() 必然 EPERM:停止/重启一律走 helper;`child.wait()` 照常。
- 旧 config/state 兼容:新字段全部 `serde(default)`;Proxy 模式行为与现状完全一致。
- 版本:2.0.0(原计划 1.1.0,合并时 1.1.x 已被 main 上 stop/restart --force 占用,升大版本)。所有回复/文案/注释用中文。
- 每个任务收尾必须 `cargo test` 全绿再 commit。

---

## 文件结构总览

| 文件 | 职责 |
|---|---|
| `src/tun.rs`(新) | TUN 常量、helper 脚本内容、参数校验、护栏解析、sudo/helper 调用封装 |
| `src/config.rs` | `TunConfig`、`Mode`、`RuntimeState.mode` |
| `src/capsule.rs` | `PortSeg { Num, Tun }`,渲染「tun」 |
| `src/daemon.rs` | `ServeArgs.tun`;隧道启停走 helper;护栏;dns-sync;shutdown 顺序 |
| `src/tunnel.rs` | `spawn_daemon` 透传 `--tun`;stop 路径 tun 分支(janitor 兜底、3s) |
| `src/install.rs` | `install --tun` / `uninstall --tun`;sudoers + visudo;补全/默认配置更新 |
| `src/lib.rs` | CLI(`connect -t`、`Uninstall`);connect 就绪检查/交互引导/janitor;胶囊适配 |

---

### Task 1: config.rs — TunConfig / Mode / RuntimeState.mode

**Files:**
- Modify: `src/config.rs`

**Interfaces:**
- Produces: `pub struct TunConfig { pub dns_suffixes: Vec<String> }`(`AppConfig.tun: TunConfig`);`pub enum Mode { Proxy, Tun }`(Copy/PartialEq/Serialize/Deserialize,Default=Proxy);`RuntimeState.mode: Mode`。

- [ ] **Step 1: 写失败测试**(加进 `src/config.rs` 的 `mod tests`)

```rust
#[test]
fn tun_config_defaults_empty_and_parses() {
    // 整段缺省
    let c: AppConfig = serde_yaml::from_str("server: s\nusername: u\n").unwrap();
    assert!(c.tun.dns_suffixes.is_empty());
    // 显式配置
    let c: AppConfig =
        serde_yaml::from_str("server: s\nusername: u\ntun:\n  dns_suffixes: [\"a.b\"]\n").unwrap();
    assert_eq!(c.tun.dns_suffixes, vec!["a.b".to_string()]);
}

#[test]
fn mode_default_is_proxy_and_legacy_state_compat() {
    assert_eq!(Mode::default(), Mode::Proxy);
    // 旧 state.json(无 mode 字段)解析后 mode 落 Proxy
    let legacy = r#"{"phase":"Online","daemon_pid":1,"port":7899,"socks_upstream":"a","http_upstream":"b","server":"s","tunnel_ip":"","error":null}"#;
    let st: RuntimeState = serde_json::from_str(legacy).unwrap();
    assert_eq!(st.mode, Mode::Proxy);
    // round-trip
    let mut st2 = st.clone();
    st2.mode = Mode::Tun;
    let back: RuntimeState = serde_json::from_str(&serde_json::to_string(&st2).unwrap()).unwrap();
    assert_eq!(back.mode, Mode::Tun);
}
```

- [ ] **Step 2: 跑测试确认编译失败**

Run: `cargo test --lib config::`
Expected: 编译错误(`TunConfig`/`Mode`/`mode` 不存在)

- [ ] **Step 3: 最小实现**

`src/config.rs`,`PromptConfig` 之后加:

```rust
/// TUN 透明模式配置(config.yaml `tun:` 段,整段可缺省)。
#[derive(Debug, Clone, Deserialize, Default)]
pub struct TunConfig {
    /// 内网专用域名后缀:隧道就绪后写成 /etc/resolver/<suffix> scoped resolver,
    /// nameserver 指服务端下发的 VPN DNS。默认空 = 不写任何 resolver 文件。
    #[serde(default)]
    pub dns_suffixes: Vec<String>,
}

/// 连接模式:Proxy=现有纯代理;Tun=分流透明模式(root 隧道 + scoped resolver)。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum Mode {
    Proxy,
    Tun,
}

impl Default for Mode {
    fn default() -> Self {
        Mode::Proxy
    }
}
```

`AppConfig` 加字段(`silent_relogin_interval` 之后):

```rust
    /// TUN 透明模式配置(仅 connect --tun 时使用)。
    #[serde(default)]
    pub tun: TunConfig,
```

`AppConfig::default()` 加 `tun: TunConfig::default(),`。

`RuntimeState` 加字段(`phase` 之后):

```rust
    /// 本次连接的模式;旧 state.json 无此字段,默认 Proxy。
    #[serde(default)]
    pub mode: Mode,
```

注意:`config.rs` 测试 `runtime_state_roundtrip_and_legacy_compat` 里的 `RuntimeState { ... }` 完整字面量要补 `mode: Mode::Proxy,`。

- [ ] **Step 4: 跑测试通过**

Run: `cargo test`
Expected: 全绿(daemon.rs 的 `RuntimeState` 字面量若报缺字段,补 `mode: if args.tun ...` 留给 Task 4——本任务先用 `mode: Mode::Proxy,` 占位保证编译)

- [ ] **Step 5: Commit**

```bash
git add src/config.rs src/daemon.rs
git commit -m "feat(config): TunConfig 与 RuntimeState.mode(默认 Proxy 兼容旧 state)"
```

---

### Task 2: capsule.rs + lib.rs — PortSeg 枚举与「tun」渲染

**Files:**
- Modify: `src/capsule.rs`、`src/lib.rs`

**Interfaces:**
- Consumes: `config::Mode`(Task 1)
- Produces: `pub enum PortSeg { Num(u16), Tun }`;`ProxyStatus.port: Option<PortSeg>`;`lib.rs` 的 `capsule_from(phase, probe, port: PortSeg)`。

- [ ] **Step 1: 写失败测试**(`src/capsule.rs` `mod tests`)

```rust
#[test]
fn tun_port_segment_renders_tun() {
    let p = PromptConfig::default();
    let st = ProxyStatus {
        state: ConnState::Online,
        delay: Delay::Value(12),
        port: Some(PortSeg::Tun),
    };
    let segs = segments(&st, &p);
    assert_eq!(segs.len(), 3);
    assert_eq!(segs[2].value, "tun");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib capsule::`
Expected: 编译错误(`PortSeg` 不存在)

- [ ] **Step 3: 实现**

`src/capsule.rs`,`ConnState` 之后:

```rust
/// 胶囊 port 段内容:Proxy 模式显示端口号,TUN 模式显示「tun」表明透明模式。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum PortSeg {
    Num(u16),
    Tun,
}
```

`ProxyStatus.port` 类型改为 `Option<PortSeg>`。`segments()` 的 port 分支改为:

```rust
    if let Some(port) = status.port {
        let value = match port {
            PortSeg::Num(n) => n.to_string(),
            PortSeg::Tun => "tun".to_string(),
        };
        out.push(Segment::new(prompt.port(), value, COLOR_PORT));
    }
```

capsule.rs 现有测试:`port: Some(7899)` 全部改 `port: Some(PortSeg::Num(7899))`。

`src/lib.rs` 同步适配(类型变更,必须一起改才能编译):
1. `use capsule::{...}` 加 `PortSeg`。
2. `capsule_from` 签名 `port: u16` → `port: PortSeg`,内部 `port: Some(port)` 保持:

```rust
fn capsule_from(phase: config::Phase, probe: Delay, port: PortSeg) -> ProxyStatus {
    match (phase, probe) {
        (config::Phase::Online, Delay::Timeout) | (config::Phase::Reconnecting, _) => ProxyStatus {
            state: ConnState::Reconnecting,
            delay: Delay::Hidden,
            port: Some(port),
        },
        (config::Phase::Online, delay) => ProxyStatus { state: ConnState::Online, delay, port: Some(port) },
    }
}
```

3. `runtime_capsule` 按 state.mode 合成:

```rust
fn runtime_capsule(st: &config::RuntimeState) -> ProxyStatus {
    let probe = if st.phase == config::Phase::Online {
        tunnel::probe_state_latency(st)
    } else {
        Delay::Hidden
    };
    let port = if st.mode == config::Mode::Tun { PortSeg::Tun } else { PortSeg::Num(st.port) };
    capsule_from(st.phase, probe, port)
}
```

4. `emit_exports` 里 `port: Some(port)` → `port: Some(PortSeg::Num(port))`。
5. lib.rs 测试里 `capsule_from(..., 7899)` → `capsule_from(..., PortSeg::Num(7899))`,断言 `c.port == Some(PortSeg::Num(7899))`;并新增一条:

```rust
#[test]
fn capsule_tun_mode_shows_tun_segment() {
    let c = capsule_from(config::Phase::Online, Delay::Value(5), PortSeg::Tun);
    assert_eq!(c.port, Some(PortSeg::Tun));
}
```

- [ ] **Step 4: 跑测试通过**

Run: `cargo test`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add src/capsule.rs src/lib.rs
git commit -m "feat(capsule): port 段 PortSeg 枚举,TUN 模式渲染「tun」"
```

---

### Task 3: src/tun.rs — 常量、helper 脚本、参数校验、护栏解析

**Files:**
- Create: `src/tun.rs`
- Modify: `src/lib.rs`(加 `mod tun;`)

**Interfaces:**
- Produces(后续任务全部依赖):
  - 常量 `HELPER_DIR/HELPER_PATH/ROOT_ZJU_PATH/SUDOERS_PATH/HELPER_SCRIPT`(均 `pub const &str`)
  - `pub fn valid_server(&str) -> bool` / `valid_twfid` / `valid_suffix` / `valid_ipv4` / `valid_bind`
  - `pub fn validate_serve(server: &str, twfid: &str, socks: &str, http: &str) -> anyhow::Result<()>`
  - `pub fn count_add_route(log: &str) -> usize`;`pub fn default_route_is_utun(route_get_output: &str) -> bool`
  - `pub fn sudo_helper(args: &[&str]) -> std::process::Command`(`/usr/bin/sudo -n HELPER_PATH <args>`)
  - `pub fn janitor() -> anyhow::Result<()>`
  - `pub enum Readiness { Ready, NotInstalled, Stale }`;`pub fn check_ready(paths: &Paths) -> Readiness`

- [ ] **Step 1: 写失败测试**(`src/tun.rs` 底部 `mod tests`,文件与实现一起建,先只放测试 + `todo!()` 也可,直接整文件写完再跑亦可——本任务按「实现+测试同文件落盘、以测试先行审视接口」执行)

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn server_twfid_bind_whitelists() {
        assert!(valid_server("vpn.example.com"));
        assert!(!valid_server("v;rm -rf /"));
        assert!(!valid_server(""));
        assert!(valid_twfid("abcDEF123"));
        assert!(!valid_twfid("a b"));
        assert!(!valid_twfid("-tun-mode")); // flag 注入样本
        assert!(valid_bind("127.0.0.1:1080"));
        assert!(!valid_bind("0.0.0.0:1080"));
        assert!(!valid_bind("127.0.0.1:1080 -x"));
    }

    #[test]
    fn suffix_and_ip_whitelists() {
        assert!(valid_suffix("corp.example.com"));
        assert!(!valid_suffix("Corp.Example.com")); // 大写不收
        assert!(!valid_suffix("a/b"));
        assert!(!valid_suffix("..")); // 路径穿越样本
        assert!(!valid_suffix(".hidden"));
        assert!(!valid_suffix("a..b"));
        assert!(valid_ipv4("10.0.104.104"));
        assert!(!valid_ipv4("10.0.104"));
        assert!(!valid_ipv4("evil"));
    }

    #[test]
    fn validate_serve_rejects_injection() {
        assert!(validate_serve("vpn.example.com", "abc123", "127.0.0.1:1080", "127.0.0.1:1081").is_ok());
        assert!(validate_serve("vpn.example.com", "x; reboot", "127.0.0.1:1080", "127.0.0.1:1081").is_err());
    }

    #[test]
    fn count_add_route_from_real_log_shape() {
        let log = "2026/07/27 10:00:01 Add route to 10.0.0.0/8\n\
                   2026/07/27 10:00:01 Add route to 172.16.0.0/12\n\
                   2026/07/27 10:00:02 SOCKS5 server listening on 127.0.0.1:1080\n";
        assert_eq!(count_add_route(log), 2);
        assert_eq!(count_add_route("no routes here"), 0);
    }

    #[test]
    fn default_route_utun_detection() {
        let utun = "   route to: default\ndestination: default\n  interface: utun6\n";
        let wifi = "   route to: default\ndestination: default\n  interface: en0\n";
        assert!(default_route_is_utun(utun));
        assert!(!default_route_is_utun(wifi));
        assert!(!default_route_is_utun("")); // 拿不到输出不误报
    }

    #[test]
    fn helper_script_invariants() {
        // 安全不变量静态自检:绝不启用 dns-hijack;标记/pidfile/root copy 路径一致;白名单存在
        assert!(!HELPER_SCRIPT.contains("dns-hijack"));
        assert!(HELPER_SCRIPT.contains("-tun-mode"));
        assert!(HELPER_SCRIPT.contains("-add-route"));
        assert!(HELPER_SCRIPT.contains("# managed by easy-proxy"));
        assert!(HELPER_SCRIPT.contains("/var/run/easy-proxy-tun.pid"));
        assert!(HELPER_SCRIPT.contains(ROOT_ZJU_PATH));
        assert!(HELPER_SCRIPT.starts_with("#!/bin/sh"));
        assert!(HELPER_SCRIPT.contains("set -eu"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib tun::`
Expected: 编译错误(模块不存在)

- [ ] **Step 3: 实现 `src/tun.rs`**

```rust
//! TUN 透明模式:root helper 的常量/脚本内容/调用封装,与护栏解析纯函数。
//!
//! 安全模型见 spec §5:sudoers 只 NOPASSWD 授权固定路径的 ep-tun-helper,
//! helper 只认白名单子命令、参数正则校验,zju-connect 命令行由 helper 自己拼。
//! 路径全部编译期固定——可配置即提权洞。

use crate::config::Paths;
use anyhow::{anyhow, Result};

pub const HELPER_DIR: &str = "/usr/local/libexec/easy-proxy";
pub const HELPER_PATH: &str = "/usr/local/libexec/easy-proxy/ep-tun-helper";
pub const ROOT_ZJU_PATH: &str = "/usr/local/libexec/easy-proxy/zju-connect";
pub const SUDOERS_PATH: &str = "/etc/sudoers.d/easy-proxy";

/// root helper 脚本全文(install --tun 释放到 HELPER_PATH,root:wheel 0755)。
pub const HELPER_SCRIPT: &str = r#"#!/bin/sh
# ep-tun-helper — easy-proxy TUN 模式 root helper(由 easy-proxy install --tun 安装)
# sudoers NOPASSWD 的唯一授权入口:子命令白名单 + 参数正则校验;
# zju-connect 命令行由本脚本固定拼装,外部只能填值、不能注入 flag。
set -eu

DIR="/usr/local/libexec/easy-proxy"
ZJU="$DIR/zju-connect"
PIDFILE="/var/run/easy-proxy-tun.pid"
RESOLVER_DIR="/etc/resolver"
MARKER="# managed by easy-proxy"

die() { echo "ep-tun-helper: $*" >&2; exit 1; }
match() { printf '%s' "$1" | grep -Eq "$2"; }

[ "$(id -u)" = "0" ] || die "需要 root(应经 sudo 调用)"

# 按 pidfile 停隧道:校验 pid 的可执行路径确为 root copy,防 pid 复用误杀
stop_tunnel() {
  [ -f "$PIDFILE" ] || return 0
  pid=$(cat "$PIDFILE" 2>/dev/null || true)
  if match "${pid:-}" '^[0-9]+$'; then
    comm=$(ps -p "$pid" -o comm= 2>/dev/null || true)
    if [ "$comm" = "$ZJU" ]; then
      kill "$pid" 2>/dev/null || true
      i=0
      while [ "$i" -lt 50 ] && kill -0 "$pid" 2>/dev/null; do sleep 0.1; i=$((i+1)); done
      if kill -0 "$pid" 2>/dev/null; then kill -9 "$pid" 2>/dev/null || true; fi
    fi
  fi
  rm -f "$PIDFILE"
}

dns_clean() {
  [ -d "$RESOLVER_DIR" ] || return 0
  for f in "$RESOLVER_DIR"/*; do
    [ -f "$f" ] || continue
    if [ "$(head -n 1 "$f" 2>/dev/null)" = "$MARKER" ]; then rm -f "$f"; fi
  done
  return 0
}

cmd="${1:-}"
if [ $# -gt 0 ]; then shift; fi

case "$cmd" in
  start-tunnel)
    server=""; https_port=""; twfid=""; socks=""; http=""
    while [ $# -gt 0 ]; do
      case "$1" in
        --server)     server="$2";     shift 2 ;;
        --https-port) https_port="$2"; shift 2 ;;
        --twfid)      twfid="$2";      shift 2 ;;
        --socks)      socks="$2";      shift 2 ;;
        --http)       http="$2";       shift 2 ;;
        *) die "未知参数: $1" ;;
      esac
    done
    match "$server" '^[A-Za-z0-9.-]+$'        || die "server 非法"
    match "$https_port" '^[0-9]+$'            || die "https-port 非法"
    match "$twfid" '^[A-Za-z0-9]+$'           || die "twfid 非法"
    match "$socks" '^127\.0\.0\.1:[0-9]+$'    || die "socks 非法"
    match "$http" '^127\.0\.0\.1:[0-9]+$'     || die "http 非法"
    stop_tunnel
    echo "$$" > "$PIDFILE"
    # exec 不换 pid,pidfile 即隧道进程;stdout/stderr 继承调用方 fd(tunnel.log)
    exec "$ZJU" -server "$server" -port "$https_port" -twf-id "$twfid" \
      -tun-mode -add-route \
      -disable-zju-config -skip-domain-resource -zju-dns-server auto -disable-multi-line \
      -socks-bind "$socks" -http-bind "$http"
    ;;
  stop-tunnel)
    stop_tunnel
    ;;
  dns-sync)
    [ $# -gt 0 ] || die "dns-sync 需要至少一个 suffix=ip"
    mkdir -p "$RESOLVER_DIR"
    keep=" "
    for pair in "$@"; do
      suffix="${pair%%=*}"; ip="${pair#*=}"
      match "$suffix" '^[a-z0-9.-]+$' || die "suffix 非法: $suffix"
      case "$suffix" in *..*|.*) die "suffix 非法: $suffix" ;; esac
      match "$ip" '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' || die "ip 非法: $ip"
      printf '%s\nnameserver %s\n' "$MARKER" "$ip" > "$RESOLVER_DIR/$suffix"
      keep="$keep$suffix "
    done
    # 同步语义:删掉带标记但不在本次列表中的旧文件
    for f in "$RESOLVER_DIR"/*; do
      [ -f "$f" ] || continue
      [ "$(head -n 1 "$f" 2>/dev/null)" = "$MARKER" ] || continue
      name=$(basename "$f")
      case "$keep" in *" $name "*) ;; *) rm -f "$f" ;; esac
    done
    ;;
  dns-clean)
    dns_clean
    ;;
  janitor)
    dns_clean
    # 只杀可执行路径 == root copy 的孤儿隧道,绝不误伤用户态 zju-connect
    pids=$(ps -axo pid=,comm= | awk -v z="$ZJU" '$2==z {print $1}' || true)
    for p in $pids; do kill "$p" 2>/dev/null || true; done
    rm -f "$PIDFILE"
    ;;
  *)
    die "未知子命令: ${cmd:-<空>}"
    ;;
esac
"#;

/// server 白名单:域名/IP 字符集(与 helper 内正则一致)。
pub fn valid_server(s: &str) -> bool {
    !s.is_empty() && s.bytes().all(|b| b.is_ascii_alphanumeric() || b == b'.' || b == b'-')
}

/// twfid 白名单:纯字母数字(与 helper 内正则一致)。
pub fn valid_twfid(s: &str) -> bool {
    !s.is_empty() && s.bytes().all(|b| b.is_ascii_alphanumeric())
}

/// resolver 后缀白名单:小写字母数字点横线,禁 `..` 与前导点(防路径穿越)。
pub fn valid_suffix(s: &str) -> bool {
    !s.is_empty()
        && s.bytes().all(|b| b.is_ascii_lowercase() || b.is_ascii_digit() || b == b'.' || b == b'-')
        && !s.contains("..")
        && !s.starts_with('.')
}

pub fn valid_ipv4(s: &str) -> bool {
    s.parse::<std::net::Ipv4Addr>().is_ok()
}

/// socks/http 监听地址白名单:必须 127.0.0.1:端口。
pub fn valid_bind(s: &str) -> bool {
    match s.strip_prefix("127.0.0.1:") {
        Some(port) => !port.is_empty() && port.bytes().all(|b| b.is_ascii_digit()),
        None => false,
    }
}

/// daemon 调 helper start-tunnel 前的整体预校验(helper 内还有同规则的最终校验)。
pub fn validate_serve(server: &str, twfid: &str, socks: &str, http: &str) -> Result<()> {
    if !valid_server(server) {
        return Err(anyhow!("server 含非法字符: {server}"));
    }
    if !valid_twfid(twfid) {
        return Err(anyhow!("twfid 含非法字符"));
    }
    if !valid_bind(socks) || !valid_bind(http) {
        return Err(anyhow!("socks/http 监听地址非法(须为 127.0.0.1:端口)"));
    }
    Ok(())
}

/// 启动护栏 a:tunnel.log 中服务端下发路由条数(0 = 服务端未下发 ipSet,TUN 不可用)。
pub fn count_add_route(log: &str) -> usize {
    log.lines().filter(|l| l.contains("Add route to")).count()
}

/// 启动护栏 b:`route -n get default` 输出的 interface 是否 utun*(是则违反分流不变量)。
pub fn default_route_is_utun(route_get_output: &str) -> bool {
    route_get_output
        .lines()
        .filter_map(|l| l.trim().strip_prefix("interface:"))
        .any(|v| v.trim().starts_with("utun"))
}

/// 组一条 `sudo -n <helper> <args...>` 命令(-n:免密不可用立即失败,绝不挂在密码提示上)。
pub fn sudo_helper(args: &[&str]) -> std::process::Command {
    let mut cmd = std::process::Command::new("/usr/bin/sudo");
    cmd.arg("-n").arg(HELPER_PATH);
    cmd.args(args);
    cmd
}

/// 同步跑一次 janitor(清残留 resolver/孤儿 root 隧道/pidfile)。
pub fn janitor() -> Result<()> {
    let out = sudo_helper(&["janitor"]).output()?;
    if !out.status.success() {
        return Err(anyhow!(
            "janitor 失败: {}",
            String::from_utf8_lossy(&out.stderr).trim()
        ));
    }
    Ok(())
}

/// TUN 组件就绪状态。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Readiness {
    /// helper 内容一致 + sudoers 存在 + root copy 与用户态 zju-connect 大小一致
    Ready,
    /// helper 与 sudoers 均不存在(全新机器)
    NotInstalled,
    /// 装过但内容/版本不一致(easy-proxy 升级后未重跑 install --tun 等)
    Stale,
}

/// 检查 TUN 组件是否就绪(不动系统,只读)。
pub fn check_ready(paths: &Paths) -> Readiness {
    let helper_exists = std::path::Path::new(HELPER_PATH).exists();
    let sudoers_exists = std::path::Path::new(SUDOERS_PATH).exists();
    if !helper_exists && !sudoers_exists {
        return Readiness::NotInstalled;
    }
    let helper_ok = std::fs::read_to_string(HELPER_PATH)
        .map(|s| s == HELPER_SCRIPT)
        .unwrap_or(false);
    let zju_ok = match (std::fs::metadata(ROOT_ZJU_PATH), std::fs::metadata(&paths.zju_bin)) {
        (Ok(root), Ok(user)) => root.len() == user.len(),
        _ => false,
    };
    if helper_ok && sudoers_exists && zju_ok {
        Readiness::Ready
    } else {
        Readiness::Stale
    }
}
```

`src/lib.rs` 模块声明区加一行 `mod tun;`(与现有 mod 按字母序排)。

- [ ] **Step 4: 跑测试通过**

Run: `cargo test`
Expected: 全绿(含 Step 1 全部新测试)

- [ ] **Step 5: Commit**

```bash
git add src/tun.rs src/lib.rs
git commit -m "feat(tun): helper 脚本常量、参数白名单校验、启动护栏解析与 sudo 封装"
```

---

### Task 4: daemon.rs — tun 分支(启停走 helper、护栏、dns-sync、shutdown 顺序)

**Files:**
- Modify: `src/daemon.rs`

**Interfaces:**
- Consumes: `tun::{HELPER_PATH, validate_serve, count_add_route, default_route_is_utun}`(Task 3)、`config::Mode`(Task 1)
- Produces: `ServeArgs.tun: bool`(`--tun` flag,Task 5 的 spawn_daemon 透传目标)

- [ ] **Step 1: `ServeArgs` 加 flag 与 state.mode**

```rust
pub struct ServeArgs {
    // ...现有字段...
    /// TUN 透明模式:隧道经 root helper 拉起
    #[arg(long, default_value_t = false)]
    tun: bool,
}
```

`run()` 里 `RuntimeState` 初始化(Task 1 占位处)改为:

```rust
        mode: if args.tun { crate::config::Mode::Tun } else { crate::config::Mode::Proxy },
```

- [ ] **Step 2: 隧道命令构造与停止统一封装**

`zju_args` 之后新增:

```rust
/// 构造隧道启动命令:Proxy 直接跑用户态 zju-connect;TUN 经 sudo -n helper(root)。
/// TUN 分支先做 Rust 侧预校验,坏参数在 spawn 前就报错(helper 内还有最终校验)。
fn tunnel_command(paths: &Paths, args: &ServeArgs, twfid: &str) -> Result<Command> {
    if args.tun {
        crate::tun::validate_serve(&args.server, twfid, &args.socks, &args.http)?;
        let mut cmd = Command::new("/usr/bin/sudo");
        cmd.arg("-n")
            .arg(crate::tun::HELPER_PATH)
            .arg("start-tunnel")
            .arg("--server").arg(&args.server)
            .arg("--https-port").arg(args.https_port.to_string())
            .arg("--twfid").arg(twfid)
            .arg("--socks").arg(&args.socks)
            .arg("--http").arg(&args.http);
        Ok(cmd)
    } else {
        let mut a = args.clone();
        a.twfid = twfid.to_string();
        let mut cmd = Command::new(&paths.zju_bin);
        cmd.args(zju_args(&a));
        Ok(cmd)
    }
}

/// 停掉隧道进程并回收:TUN 模式 daemon 无权 signal root 子进程(EPERM),
/// 一律走 helper stop-tunnel(pidfile);Proxy 维持 start_kill。两者都必须 wait 回收。
async fn kill_tunnel(child: &mut Child, tun: bool) {
    if tun {
        let _ = Command::new("/usr/bin/sudo")
            .args(["-n", crate::tun::HELPER_PATH, "stop-tunnel"])
            .output()
            .await;
    } else {
        let _ = child.start_kill();
    }
    let _ = child.wait().await;
}
```

替换所有直接 `child.start_kill()`(+ 其后的 `wait`)调用点:
- `run()` 里 `wait_socks_ready` 失败分支、bind 失败分支、循环结束后的清理 → `kill_tunnel(&mut child, args.tun).await`
- `restart_zju` 开头的回收 → `kill_tunnel(child, args.tun).await`
- `run()`/`restart_zju` 的 spawn 改用 `tunnel_command(paths, &args, &twfid)?.stdin(...).stdout(...).stderr(...).spawn()`(重定向到 tunnel.log 的写法保持现状)

- [ ] **Step 3: 启动护栏 + dns-sync**

新增两个函数:

```rust
/// TUN 启动护栏(spec §7.1 第 7 步):就绪后立即执行,失败绝不假 online。
/// a) 服务端未下发网段(Add route to == 0)→ TUN 无意义;
/// b) 默认路由接口是 utun* → 违反分流不变量(防上游行为变化)。
fn tun_guardrails(paths: &Paths) -> Result<()> {
    let log = crate::config::read_tail_bytes(&paths.tunnel_log, 512 * 1024);
    if crate::tun::count_add_route(&log) == 0 {
        return Err(anyhow!("服务端未下发内网网段(tunnel.log 无 Add route to),TUN 不可用"));
    }
    if let Ok(out) = std::process::Command::new("/sbin/route").args(["-n", "get", "default"]).output() {
        if crate::tun::default_route_is_utun(&String::from_utf8_lossy(&out.stdout)) {
            return Err(anyhow!("默认路由指向 utun,违反分流不变量,回滚"));
        }
    }
    Ok(())
}

/// 隧道就绪后同步 scoped resolver(幂等):dns_suffixes 非空且拿到 vpn_dns 才写;
/// 失败只警告不致命——resolver 缺失时内网域名仍可走 7899 代理路径。
async fn tun_dns_sync(cfg: &AppConfig, state: &RuntimeState) {
    let suffixes: Vec<&str> = cfg
        .tun
        .dns_suffixes
        .iter()
        .map(String::as_str)
        .filter(|s| {
            let ok = crate::tun::valid_suffix(s);
            if !ok {
                eprintln!("[daemon] dns_suffixes 含非法项,跳过: {s}");
            }
            ok
        })
        .collect();
    if suffixes.is_empty() {
        return;
    }
    let Some(dns) = state.vpn_dns.as_deref().filter(|d| crate::tun::valid_ipv4(d)) else {
        eprintln!("[daemon] 未获得可用 VPN DNS,跳过 scoped resolver 配置(域名解析可走 7899 代理)");
        return;
    };
    let mut cmd = Command::new("/usr/bin/sudo");
    cmd.arg("-n").arg(crate::tun::HELPER_PATH).arg("dns-sync");
    for s in &suffixes {
        cmd.arg(format!("{s}={dns}"));
    }
    match cmd.output().await {
        Ok(out) if out.status.success() => {
            eprintln!("[daemon] scoped resolver 已同步: {} 个后缀 → {dns}", suffixes.len());
        }
        Ok(out) => eprintln!(
            "[daemon] dns-sync 失败: {}",
            String::from_utf8_lossy(&out.stderr).trim()
        ),
        Err(e) => eprintln!("[daemon] dns-sync 无法执行: {e}"),
    }
}
```

接线:
1. `run()` 中 `wait_socks_ready` 成功分支之后、bind 之前,`args.tun` 时:护栏失败 → `kill_tunnel` + `state.error` 落盘 + return Err;护栏通过 → `tun_dns_sync(&cfg, &state).await`。

```rust
    if args.tun {
        if let Err(e) = tun_guardrails(paths) {
            kill_tunnel(&mut child, true).await;
            state.error = Some(e.to_string());
            let _ = paths.write_state(&state);
            return Err(e);
        }
        tun_dns_sync(&cfg, &state).await;
    }
```

2. `attempt_recover` 的两处「探测通 → Online」判定,在 `probe(...)` 通过之后补 TUN 护栏与 dns-sync(护栏失败按恢复失败处理):

```rust
                if probe(args, state.vpn_dns.as_deref()).await {
                    if args.tun {
                        if let Err(e) = tun_guardrails(paths) {
                            eprintln!("[daemon] 恢复后护栏失败: {e}");
                            return RecoverOutcome::GiveUp;
                        }
                        tun_dns_sync(cfg, state).await;
                    }
                    eprintln!("[daemon] 旧 TWFID 重启成功");
                    return RecoverOutcome::Online;
                }
```

(静默重登成功那处同样处理,GiveUp 语义一致。)

3. shutdown 顺序(spec §7.2):循环结束后的清理改为:

```rust
    if let Some(c) = route_child.as_mut() {
        let _ = c.start_kill();
    }
    kill_tunnel(&mut child, args.tun).await;
    if args.tun {
        // 优雅退出才清 resolver;崩溃残留由下次 janitor 兜底
        let _ = Command::new("/usr/bin/sudo")
            .args(["-n", crate::tun::HELPER_PATH, "dns-clean"])
            .output()
            .await;
    }
    paths.clear_state();
```

- [ ] **Step 4: 跑全量测试 + 构建**

Run: `cargo test && cargo build`
Expected: 全绿(本任务无新增单测——护栏/校验纯函数已在 Task 3 覆盖,本处是接线;`relay`/路由事件等现有测试不得回归)

- [ ] **Step 5: Commit**

```bash
git add src/daemon.rs
git commit -m "feat(daemon): TUN 分支——隧道启停走 helper、启动护栏、dns-sync、shutdown 序列"
```

---

### Task 5: tunnel.rs — spawn_daemon 透传 --tun,stop 路径 tun 分支

**Files:**
- Modify: `src/tunnel.rs`

**Interfaces:**
- Consumes: `config::Mode`、`tun::{sudo_helper}`
- Produces: `pub fn spawn_daemon(paths, cfg, twfid, last_sms_sent, tun: bool) -> Result<()>`;`stop_daemon`/`stop_daemon_and_wait` 行为(Task 6/7 的 uninstall 与 lib.rs 调用方依赖)

- [ ] **Step 1: 实现**

`spawn_daemon` 签名加 `tun: bool`,构参处:

```rust
    cmd.arg("__serve")
        // ...现有参数...
        .arg("--last-sms-sent").arg(last_sms_sent.to_string());
    if tun {
        cmd.arg("--tun");
    }
```

`stop_daemon_and_wait` 改为按 state.mode 分流兜底(pkill 对 root 进程无效):

```rust
pub fn stop_daemon_and_wait(paths: &Paths, timeout: Duration) {
    let tun_mode = paths
        .read_state()
        .map(|s| s.mode == crate::config::Mode::Tun)
        .unwrap_or(false);
    if let Some(st) = paths.read_state() {
        if st.daemon_pid > 0 && pid_alive(st.daemon_pid) {
            unsafe {
                libc::kill(st.daemon_pid, libc::SIGTERM);
            }
            let deadline = Instant::now() + timeout;
            while pid_alive(st.daemon_pid) && Instant::now() < deadline {
                std::thread::sleep(Duration::from_millis(100));
            }
        }
    }
    if tun_mode {
        // 兜底:daemon 没走完优雅路径时,root 隧道/resolver/pidfile 由 janitor 收拾
        let _ = crate::tun::sudo_helper(&["janitor"]).output();
    } else {
        // 兜底:清掉可能残留的、由我们释放的用户态 zju-connect
        let _ = Command::new("pkill")
            .arg("-f")
            .arg(paths.zju_bin.display().to_string())
            .output();
    }
    paths.clear_state();
}

/// 停止守护,供 disconnect 使用:TUN 模式要等 stop-tunnel → dns-clean 走完,给 3s;
/// Proxy 维持 300ms。
pub fn stop_daemon(paths: &Paths) {
    let tun_mode = paths
        .read_state()
        .map(|s| s.mode == crate::config::Mode::Tun)
        .unwrap_or(false);
    let timeout = if tun_mode { Duration::from_secs(3) } else { Duration::from_millis(300) };
    stop_daemon_and_wait(paths, timeout);
}
```

同文件 `spawn_daemon` 的调用方在 lib.rs(下一任务改);先改 lib.rs 现有调用点补 `false`…——不,lib.rs 的 connect 改动集中在 Task 7,此处为保编译,直接把 lib.rs `tunnel::spawn_daemon(paths, cfg, &twfid, last_sms.get())` 改成 `tunnel::spawn_daemon(paths, cfg, &twfid, last_sms.get(), false)`(Task 7 再改成传真实 tun)。

- [ ] **Step 2: 跑全量测试**

Run: `cargo test`
Expected: 全绿

- [ ] **Step 3: Commit**

```bash
git add src/tunnel.rs src/lib.rs
git commit -m "feat(tunnel): spawn_daemon 透传 --tun;stop 路径 TUN 分支走 janitor 兜底(3s)"
```

---

### Task 6: install.rs — install --tun 与 uninstall --tun

**Files:**
- Modify: `src/install.rs`、`src/lib.rs`(CLI 变体)

**Interfaces:**
- Consumes: `tun::{HELPER_DIR, HELPER_PATH, ROOT_ZJU_PATH, SUDOERS_PATH, HELPER_SCRIPT, janitor}`
- Produces: `pub fn cmd_install(paths: &Paths, tun: bool) -> Result<()>`;`pub fn install_tun(paths: &Paths) -> Result<()>`(Task 7 connect 引导复用);`pub fn cmd_uninstall(paths: &Paths, tun: bool) -> Result<()>`

- [ ] **Step 1: 写失败测试**(`src/install.rs` `mod tests`)

```rust
#[test]
fn sudoers_line_shape() {
    let line = sudoers_content("alice");
    assert_eq!(line, "alice ALL=(root) NOPASSWD: /usr/local/libexec/easy-proxy/ep-tun-helper\n");
}

#[test]
fn completion_covers_tun_surface() {
    let s = completion_script();
    assert!(s.contains("uninstall:"));
    assert!(s.contains("--tun"));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib install::`
Expected: `sudoers_content` 不存在 → 编译失败

- [ ] **Step 3: 实现**

CLI(`src/lib.rs`):

```rust
    /// 安装 .zshrc wrapper、tab 补全与默认配置
    Install {
        /// 同时安装 TUN 权限组件(root helper + sudoers,需一次 sudo 密码)
        #[arg(long)]
        tun: bool,
    },
    /// 卸载 TUN 权限组件(helper / root copy / sudoers,需一次 sudo 密码)
    Uninstall {
        /// 目前仅支持 --tun
        #[arg(long)]
        tun: bool,
    },
```

`run()` 里 Install/Uninstall 都在 `read_app_config` 之前处理:

```rust
    if let Commands::Install { tun } = &cli.command {
        return install::cmd_install(&paths, *tun);
    }
    if let Commands::Uninstall { tun } = &cli.command {
        return install::cmd_uninstall(&paths, *tun);
    }
```

(match 底部 `Commands::Install ... => unreachable!()` 分支同步改成带字段的模式。)

`src/install.rs`:

```rust
pub fn cmd_install(paths: &Paths, tun: bool) -> Result<()> {
    // ...现有 install 逻辑不动...
    if tun {
        install_tun(paths)?;
    }
    Ok(())
}

/// sudoers 单行内容:只 NOPASSWD 授权固定路径的 helper 这一个入口。
fn sudoers_content(user: &str) -> String {
    format!("{user} ALL=(root) NOPASSWD: {}\n", crate::tun::HELPER_PATH)
}

fn current_username() -> Result<String> {
    std::env::var("USER")
        .ok()
        .filter(|s| !s.is_empty())
        .or_else(|| {
            Command::new("/usr/bin/id")
                .arg("-un")
                .output()
                .ok()
                .filter(|o| o.status.success())
                .map(|o| String::from_utf8_lossy(&o.stdout).trim().to_string())
        })
        .filter(|s| !s.is_empty())
        .ok_or_else(|| anyhow::anyhow!("无法确定当前用户名"))
}

/// TUN 权限组件安装(spec §5.1):单次交互 sudo 完成目录/两个文件/sudoers,末尾免密自检。
pub fn install_tun(paths: &Paths) -> Result<()> {
    ensure_zju_bin(paths)?; // root copy 的来源必须先就位
    let cache = &paths.cache_dir;
    fs::create_dir_all(cache)?;
    let helper_tmp = cache.join("ep-tun-helper.staged");
    fs::write(&helper_tmp, crate::tun::HELPER_SCRIPT)?;
    let sudoers_tmp = cache.join("sudoers.staged");
    fs::write(&sudoers_tmp, sudoers_content(&current_username()?))?;

    // 一段 root shell 完成全部落盘;路径经位置参数传入,不做字符串拼接
    let script = r#"set -e
install -d -o root -g wheel -m 0755 "$1"
install -o root -g wheel -m 0755 "$2" "$1/ep-tun-helper"
install -o root -g wheel -m 0755 "$3" "$1/zju-connect"
visudo -cf "$4"
install -o root -g wheel -m 0440 "$4" /etc/sudoers.d/easy-proxy
"#;
    eprintln!("  安装 TUN 权限组件需要 root,请输入 sudo 密码(仅此一次):");
    let status = Command::new("/usr/bin/sudo")
        .arg("/bin/sh").arg("-c").arg(script).arg("sh")
        .arg(crate::tun::HELPER_DIR)
        .arg(&helper_tmp)
        .arg(&paths.zju_bin)
        .arg(&sudoers_tmp)
        .status()
        .context("无法执行 sudo")?;
    let _ = fs::remove_file(&helper_tmp);
    let _ = fs::remove_file(&sudoers_tmp);
    if !status.success() {
        return Err(anyhow::anyhow!("TUN 组件安装失败(sudo 退出 {status})"));
    }
    // 自检:免密路径立即可用(顺带完成一次 janitor 清场)
    crate::tun::janitor().context("安装自检失败:sudo -n 免密调用 helper 不可用")?;

    let prompt = PromptConfig::default();
    println!("{}", success_line(&format!("已安装 root helper: {}", crate::tun::HELPER_PATH), None, &prompt));
    println!("{}", success_line(&format!("已安装 sudoers 免密规则: {}", crate::tun::SUDOERS_PATH), None, &prompt));
    println!("{}", success_line("TUN 组件就绪(免密自检通过)", None, &prompt));
    Ok(())
}

/// 卸载 TUN 权限组件(spec §5.3):对称移除,幂等,不依赖 helper 完好。
pub fn cmd_uninstall(paths: &Paths, tun: bool) -> Result<()> {
    if !tun {
        return Err(anyhow::anyhow!("目前仅支持 uninstall --tun"));
    }
    let prompt = PromptConfig::default();
    // 先停 TUN 模式 daemon(优雅路径会自己 stop-tunnel + dns-clean)
    if let Some(st) = paths.read_state() {
        if st.mode == crate::config::Mode::Tun && crate::tunnel::pid_alive(st.daemon_pid) {
            eprintln!("  检测到 TUN 连接,先断开…");
            crate::tunnel::stop_daemon_and_wait(paths, std::time::Duration::from_secs(3));
        }
    }
    let helper_installed = Path::new(crate::tun::HELPER_DIR).exists();
    let sudoers_installed = Path::new(crate::tun::SUDOERS_PATH).exists();
    if !helper_installed && !sudoers_installed {
        println!("{}", success_line("TUN 组件未安装,无需卸载", None, &prompt));
        return Ok(());
    }
    // root 内联清理:等价 janitor 但不依赖 helper 完好(半残安装也能卸干净)
    let script = r#"set -u
if [ -d /etc/resolver ]; then
  for f in /etc/resolver/*; do
    [ -f "$f" ] || continue
    if [ "$(head -n 1 "$f" 2>/dev/null)" = "# managed by easy-proxy" ]; then rm -f "$f"; fi
  done
fi
pids=$(ps -axo pid=,comm= | awk '$2=="/usr/local/libexec/easy-proxy/zju-connect" {print $1}')
for p in $pids; do kill "$p" 2>/dev/null || true; done
rm -f /var/run/easy-proxy-tun.pid
rm -f /etc/sudoers.d/easy-proxy
rm -rf /usr/local/libexec/easy-proxy
exit 0
"#;
    eprintln!("  卸载 TUN 权限组件需要 root,请输入 sudo 密码:");
    let status = Command::new("/usr/bin/sudo")
        .arg("/bin/sh").arg("-c").arg(script)
        .status()
        .context("无法执行 sudo")?;
    if !status.success() {
        return Err(anyhow::anyhow!("卸载失败(sudo 退出 {status})"));
    }
    if helper_installed {
        println!("{}", success_line(&format!("已删除 {}", crate::tun::HELPER_DIR), None, &prompt));
    }
    if sudoers_installed {
        println!("{}", success_line(&format!("已删除 {}", crate::tun::SUDOERS_PATH), None, &prompt));
    }
    println!("{}", success_line("TUN 组件已卸载(resolver 残留与孤儿隧道一并清理)", None, &prompt));
    Ok(())
}
```

注意:`install.rs` 顶部需要 `use anyhow::Context;`(已有 `Context` 则不动);`Path` 已在 use 里。

补全脚本 `completion_script()` 更新:

```zsh
    'install:安装 .zshrc、补全与默认配置(--tun 加装 TUN 权限组件)'
    'uninstall:卸载 TUN 权限组件'
```

args 分支:

```zsh
        connect) _arguments '--relogin[忽略钥匙串密码,强制重新输入]' '(-t --tun)'{-t,--tun}'[以 TUN 透明模式连接]' ;;
        install) _arguments '--tun[安装 TUN 权限组件(需一次 sudo)]' ;;
        uninstall) _arguments '--tun[卸载 TUN 权限组件(需一次 sudo)]' ;;
```

默认配置 `default_config()` 在 `prompt:` 段之前加:

```yaml
# TUN 透明模式(connect --tun)专用:内网专用域名后缀,隧道在线时写成 /etc/resolver/<suffix>
# 分流解析(nameserver 指服务端下发的 VPN DNS);只放内网 zone,别把纯公网域名圈进来。
# tun:
#   dns_suffixes: ["corp.example.com"]
```

- [ ] **Step 4: 跑测试通过**

Run: `cargo test`
Expected: 全绿(Step 1 两条 + 现有测试)

- [ ] **Step 5: Commit**

```bash
git add src/install.rs src/lib.rs
git commit -m "feat(install): install --tun / uninstall --tun(root helper + sudoers + 免密自检,幂等卸载)"
```

---

### Task 7: lib.rs — connect --tun 流程(就绪检查、交互引导安装、janitor、模式互斥)

**Files:**
- Modify: `src/lib.rs`

**Interfaces:**
- Consumes: `tun::{check_ready, Readiness, janitor}`、`install::install_tun`(Task 6)、`tunnel::spawn_daemon(..., tun)`(Task 5)
- Produces: `Commands::Connect { relogin, tun }`;`cmd_connect(paths, cfg, relogin, tun)`

- [ ] **Step 1: CLI 与流程实现**

`Commands::Connect` 加 flag:

```rust
    Connect {
        /// 忽略钥匙串中已存密码,强制重新输入
        #[arg(long)]
        relogin: bool,
        /// TUN 透明模式:内网网段免代理直达、内网域名分流解析(需先 install --tun)
        #[arg(long, short = 't')]
        tun: bool,
    },
```

`run()` 分发:`Commands::Connect { relogin, tun } => cmd_connect(&paths, &cfg, relogin, tun)`。

`cmd_connect` 签名加 `tun: bool`,在「已有守护」检查处加模式互斥,在 `ensure_zju_bin` 之后、登录之前加就绪检查与 janitor:

1. 已有守护且 `st.phase == Online` 的分支开头加:

```rust
                let running_tun = st.mode == config::Mode::Tun;
                if running_tun != tun {
                    println!(
                        "{}",
                        info_line(
                            &format!(
                                "已有{}模式连接,如需切换请先 easy-proxy disconnect",
                                if running_tun { " TUN " } else { "代理" }
                            ),
                            Some(&runtime_capsule(&st)),
                            &cfg.prompt,
                        )
                    );
                    return Ok(());
                }
```

2. `install::ensure_zju_bin(paths)?;` 之后:

```rust
    if tun {
        ensure_tun_ready(paths)?;
        // 清上次残留:resolver 文件、孤儿 root 隧道、pidfile(spec §7.1 第 3 步)
        tun::janitor()?;
    }
```

3. 新增函数(`cmd_connect` 附近):

```rust
/// TUN 就绪检查(spec §7.1 第 2 步):组件缺失/过旧时交互引导当场安装;
/// 无 tty(自动化)不询问,直接报错退出。
fn ensure_tun_ready(paths: &Paths) -> Result<()> {
    let readiness = tun::check_ready(paths);
    if readiness == tun::Readiness::Ready {
        return Ok(());
    }
    let what = match readiness {
        tun::Readiness::NotInstalled => "TUN 组件未安装",
        _ => "TUN 组件版本过旧",
    };
    if !std::io::stdin().is_terminal() {
        return Err(anyhow!("{what},请先执行 easy-proxy install --tun"));
    }
    let yes = prompt_or_exit(
        dialoguer::Confirm::with_theme(&ep_theme())
            .with_prompt(format!("{what},现在安装吗?(需输入一次 sudo 密码)"))
            .default(true)
            .interact(),
    )?;
    if !yes {
        return Err(anyhow!("已取消。可稍后手动执行 easy-proxy install --tun"));
    }
    install::install_tun(paths)?;
    Ok(())
}
```

4. `spawn_daemon` 调用点改传真实值:`tunnel::spawn_daemon(paths, cfg, &twfid, last_sms.get(), tun)?;`

- [ ] **Step 2: 跑全量测试 + 构建**

Run: `cargo test && cargo build`
Expected: 全绿

- [ ] **Step 3: 冒烟(不碰真机 VPN)**

```bash
cargo run -- connect --help   # 应显示 -t, --tun
cargo run -- uninstall        # 应报错「目前仅支持 uninstall --tun」
echo | cargo run -- connect --tun 2>&1 | head -5   # 无 tty 且未安装 → 报错提示 install --tun(需 config 已填 server/username 才走到该分支;本机已有配置)
```

Expected: 帮助/报错文案正确,无 panic

- [ ] **Step 4: Commit**

```bash
git add src/lib.rs
git commit -m "feat(connect): --tun/-t——就绪检查交互引导安装、janitor 前置、双模式互斥"
```

---

### Task 8: README、版本号与收尾

**Files:**
- Modify: `README.md`、`Cargo.toml`

- [ ] **Step 1: Cargo.toml 版本 1.0.0 → 1.1.0**

- [ ] **Step 2: README 增补 TUN 模式章节**(插入到现有使用说明之后,措辞可随 README 现有风格微调,要点必须齐):

```markdown
## TUN 透明模式(macOS)

`connect --tun`(或 `-t`)以分流 TUN 模式连接:服务端下发的内网网段直接走 utun 虚拟网卡,
配置的内网域名后缀由 VPN DNS 分流解析(/etc/resolver scoped resolver),浏览器/ssh 等
无需配置代理即可直访内网。mixed 7899 与 `start/stop/restart` 语义完全不变。

- 首次使用需 `easy-proxy install --tun`(一次 sudo 密码,安装 root helper 与 sudoers 免密规则;
  connect --tun 检测到未安装会交互引导)。easy-proxy 升级后需重跑一次以刷新 root 副本。
- 卸载:`easy-proxy uninstall --tun`(删除 /usr/local/libexec/easy-proxy 与 /etc/sudoers.d/easy-proxy,
  清理 resolver 残留与孤儿隧道,幂等)。
- config.yaml 配置内网域名后缀(可选):

  ```yaml
  tun:
    dns_suffixes: ["corp.example.com"]
  ```

- 安全边界:全程不改默认路由、不改全局 DNS(不用 dns-hijack);utun 与分流路由由内核随进程
  消亡自动清理,任何异常退出(kill -9/断电)不破坏公网网络,残留 resolver 文件下次使用时自动清理。
- 验证 scoped resolver 用 `dscacheutil -q host -a name 内网域名` 或 ping;`dig/nslookup`
  不走系统解析器,会误判「没生效」。
```

- [ ] **Step 3: 全量验证**

Run: `cargo test && cargo build --release`
Expected: 全绿、构建成功

- [ ] **Step 4: Commit**

```bash
git add README.md Cargo.toml Cargo.lock
git commit -m "docs+chore: README TUN 模式章节,版本 1.1.0"
```

---

## 真机验证清单(实现完成后人工执行,spec §12)

代码全部落地后,以下 9 项须在真机逐项验证(不属于本计划的自动化步骤,结果记入项目记忆):

1. 服务端是否下发 ipSet:`sudo zju-connect -tun-mode -add-route ...` 看 `Add route to` 条数(先决条件,已由用户在另一会话发起);
2. kill -9 root zju-connect 后 `netstat -rn` 路由自清、公网无感;
3. TUN 模式下 SOCKS 探针仍测隧道数据面;
4. MTU 1400 内网 git clone;
5. 与 clash verge(系统代理模式)共存;
6. `dscacheutil -q host` 验证域名分流;Docker 容器内解析;
7. 看门狗无人值守免密重启(sudo -n 无 tty)端到端;
8. 钥匙串静默重登无新增弹框;
9. `uninstall --tun` 三件套移除干净、再 `connect --tun` 能重新引导安装。
